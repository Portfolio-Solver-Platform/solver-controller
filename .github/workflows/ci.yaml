name: CI
on:
  pull_request:
  workflow_call:

permissions:
  contents: read

jobs:
  ci:
    permissions:
      contents: read

    uses: Portfolio-Solver-Platform/infrastructure/.github/workflows/python-ci.yaml@8e77fb9b4cbcee0182ce58eb20846c108b561728 # main
    with:
      code_audit_targets: "./src/"
